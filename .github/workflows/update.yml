name: Copier Update

on:
  workflow_dispatch:

jobs:
  copier:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: install copier
        run: pipx install copier
      - name: copier update
        run: copier -f update
